# Copy to FlightBox/.env (next to this folder) and fill in real values.
# Do not put real secrets in this file — it is committed to Git.

CLIENT_ID=
CLIENT_SECRET=
TENANT_ID=
MAILBOX_EMAIL=
# GRAPH_API_ENDPOINT=https://graph.microsoft.com/v1.0
